[project]
name = "ecomma"
version = "0.1.0"

dependencies = [
    "pandas",
    "pandera",
    "openpyxl",
    "faker",
    "requests",
    "supabase",
    "python-dotenv"
]

[tool.setuptools]
package-dir = {"" = "src"}